import React from 'react';
import styled from "styled-components";

const Wrapper = styled.div`
  display:grid;
  grid-template-rows: repeat(7, 1fr);
  border: 3px solid tomato;
`;

export default function MonthlyCalendarWeek () {
  //프롭 받아서 깔기
// 7일치 깔기
  return (
    <Wrapper>
     
    </Wrapper>
  );
}